package HelloWorld;

public class HelloWorld { //class definition
    //main method
    public static void main(String[] args) { // access modifier + doesn't return value + name of method + arguments
        //executable, invokes all the other methods
        System.out.println("Hello World"); //prints expression on console
        System.out.println("size of args = " + args.length);
    }
}